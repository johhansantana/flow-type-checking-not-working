// @flow
import React from "react";
import TypeTest from './TypeTest'

// Try ctrl + space to see suggestions here it won't give you the correct ones
// and it won't typecheck either
export default () => <TypeTest title={{}} />;
